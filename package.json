{
  "name": "resultset-backend",
  "version": "1.0.0",
  "description": "Backend API for Railway",
  "main": "server.js",
  "scripts": {
    "start": "npm install && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}